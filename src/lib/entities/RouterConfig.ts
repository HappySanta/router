export interface RouterConfig {
  enableLogging?: boolean,
  enableErrorThrowing?: boolean,
  defaultPage?: string,
  defaultView?: string,
  defaultPanel?: string,
  defaultRoot?: string,
  noSlash?: boolean,
}